<resources>
    <!-- Окно только задаёт фон и системные панели: всё остальное рисует Compose. -->
    <style name="Theme.SSTP" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>
